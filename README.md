# Smart Digital Store System

Foundation scaffold. Placeholder UI only — final design pending Figma.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

Open http://localhost:3000

## Notes

- Mock data lives in `lib/mock-data.ts`.
- Supabase, AI, and Telegram clients are stubbed and not connected.
- DB schema: `supabase/schema.sql`.
# Smart-Store-Management
